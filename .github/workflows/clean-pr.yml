
name: Clean
on:
    pull_request:
        branches: [ "2025" ]
    workflow_dispatch:
        inputs:
            pr_number:
                description: 'the pr number'
                required: true
                type: string

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  spotless:
    # The type of runner that the job will run on
    runs-on: ubuntu-22.04
    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Checkout PR (reg)
        if: ${{ github.event_name == 'pull_request' }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh pr checkout ${{ github.event.pull_request.number }}
      - name: Checkout PR (successive)
        if: ${{ github.event_name == 'workflow_dispatch' }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh pr checkout ${{ github.event.inputs.pr_number }}
      - uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: 17
      - name: Spotless Apply
        run: ./gradlew spotlessApply
      - name: Commit
        id: commit
        continue-on-error: true
        run: |
          git config --global user.name 'Spotless Commiter'
          git config --global user.email 'SPoovathumkadavil@users.noreply.github.com'
          git commit -am "Clean '${{ github.event.head_commit.message }}'"
      - name: Push PR
        if: ${{ steps.commit.outcome == 'success' }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git push
          echo "Push successful"
      - name: Trigger Workflow Again
        if: ${{ steps.commit.outcome == 'success' }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh workflow run clean-pr.yml -f pr_number=${{ github.event.pull_request.number }}
